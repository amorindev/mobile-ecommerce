import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  static const Color blueColor = Colors.blueAccent;
  static const Color redColor = Colors.red;
  static const Color redColord = Color.fromRGBO(255, 82, 82, 1);
  static const Color orangeColor = Colors.orange;

  static const Color blackColor = Colors.black;
  static const Color greenColor = Colors.green;
  static const Color whiteColor = Colors.white;
  // mejor un color const
  static final Color greyColor = Colors.grey[600] ?? Colors.grey;
  static const Color grey2Color = Color.fromARGB(255, 211, 210, 210);

  static const Color brandColor = Colors.redAccent;
  static const Color linkColor = Colors.blueAccent;
}
