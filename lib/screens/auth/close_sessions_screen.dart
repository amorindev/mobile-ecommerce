import 'package:flutter/material.dart';

class CloseSessionsScreen extends StatelessWidget {
  const CloseSessionsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: ListView()),
    );
  }
}
